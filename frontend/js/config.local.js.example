/**
 * Local development configuration example
 * Copy this file to config.js and update the API_BASE_URL for local development
 * Or set window.API_BASE_URL before config.js is loaded
 */

// Local development API URL (serverless-offline runs on port 3000)
const API_BASE_URL = 'http://localhost:3000/dev';

window.API_CONFIG = {
  API_BASE_URL: API_BASE_URL
};
